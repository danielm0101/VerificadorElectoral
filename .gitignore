# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
dist-electron/
build/
!build/installer.nsh
*.exe
*.msi

# IDE
.idea/
.vscode/
*.swp
*.swo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS files
.DS_Store
Thumbs.db

# Environment
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Config con API keys (NO commitear)
config.json

# Misc
*.pem
coverage/

# R Portable (muy grande para git, se distribuye con el instalador)
r-portable/

# Python Portable (muy grande para git, se genera con: npm run setup:python)
python-portable/*
!python-portable/.gitkeep

# Security - master encryption key (NEVER commit)
llave_maestra.key

# Double-key fragments (NEVER commit)
usb_key.bin
github_key.bin

# Encrypted script files (generated by security:encrypt)
r-scripts/**/*.enc

# Decrypted R scripts and CSVs (only .enc goes to production)
r-scripts/**/*.R
r-scripts/**/*.csv

# R libraries (local dev)
library/

# Large Excel data file (use generar_divipole.py to regenerate)
*.xlsx

# Embedded repo
redirect-registraduria/

# USB registry (sensitive)
electron/security/registry.json
